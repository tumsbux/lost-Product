# เอกสารสถาปัตยกรรมฐานข้อมูลและ Data Dictionary: ระบบ GP Analysis Dashboard

สถานะ: CURRENT — ตรวจสอบจากโค้ดจริง [`F:\lost-Product\build_gp_analysis.py`, `F:\lost-Product\gp_analysis_data.json`] ณ วันที่ 2026-09-04

---

## 1. ภาพรวมการเชื่อมต่อฐานข้อมูล (Database Connection Overview)

ระบบประมวลผลข้อมูลของ GP Analysis Dashboard (`build_gp_analysis.py`) มีการเชื่อมต่อไปยังฐานข้อมูลหลักบน MySQL Server (Host ตามคอนฟิก `db_config.json` พอร์ต 13306) จำนวน **2 ฐานข้อมูล** และสร้างคลังข้อมูลระดับไฟล์แบบ Snapshot (**1 Local Datamart**) ดังนี้:

```
+-----------------------------------------------------------------------------------+
|                                 MySQL Server                                      |
|                                                                                   |
|   +----------------------------+             +--------------------------------+   |
|   |    Database: data-lake     |             |  Database: MYPOS2018_CENTER    |   |
|   |                            |             |                                |   |
|   | - fact_sales               |             | - item_group                   |   |
|   | - dim_branch               |             | - item_type                    |   |
|   | - dim_product              |             | - item_barcode                 |   |
|   +-------------+--------------+             +---------------+----------------+   |
|                 | (READ-ONLY)                                | (READ-ONLY)        |
+-----------------|--------------------------------------------|--------------------+
                  |                                            |
                  +---------------------+----------------------+
                                        |
                                        v
                       +---------------------------------+
                       |  Python ETL Pipeline            |
                       |  (build_gp_analysis.py)         |
                       +----------------+----------------+
                                        |
                                        v
                       +---------------------------------+
                       |  Local Analytical Datamart      |
                       |  (gp_analysis_data.json ~8.3MB) |
                       +----------------+----------------+
                                        |
                                        v
                       +---------------------------------+
                       |  Client-side Browser Dashboard  |
                       |  (gp_analysis_dashboard.html)   |
                       +----------------+----------------+
```

---

## 2. รายละเอียดฐานข้อมูลที่เกี่ยวข้อง (Database Specifications)

### 2.1 ฐานข้อมูล: `data-lake`
- **สิทธิ์การเข้าถึง:** **READ-ONLY 100%** (SELECT อย่างเดียว ไม่มีการ INSERT/UPDATE/DELETE)
- **Charset / Collation:** `utf8mb4`
- **บทบาททางสถาปัตยกรรม:** เป็นแกนหลัก (Data Backbone) สำหรับเก็บประวัติการทำรายการขายระดับบิลย่อยหน้าร้าน และข้อมูล Master สินค้า/สาขา

#### ตารางที่ 1: `data-lake.fact_sales` (ตาราง Fact การขายหน้าร้าน)
*บทบาท:* บันทึกรายการขายสินค้าทุกบรรทัดบิลที่เกิดขึ้นในสาขา

| ชื่อคอลัมน์จริง | ชนิดข้อมูล | บทบาทและหน้าที่ในระบบ | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `sodate` | `date / datetime` | วันที่ทำรายการขาย ใช้กรองช่วงเวลา (`sodate >= %s`) และจัดกลุ่มรายเดือน (`mo`) | `'2026-09-02'` |
| `sono` | `varchar` | เลขที่บิลขายหน้าร้าน ใช้ในแท็บ Audit บิลผิดปกติ | `'BL0311-260901-0021'` |
| `sotowhs` | `varchar` | รหัสสาขาที่เกิดการขาย ใช้ฟอร์แมต 3 หลัก (`zfill(3)`) เพื่อผูกกับ `dim_branch` | `'031'`, `'311'` |
| `iprod` | `varchar` | รหัสสินค้าของรายการนั้นๆ ใช้เชื่อมโยงกับ Master สินค้า | `'8855199167018'` |
| `soqty` | `decimal` | จำนวนชิ้นที่ขายได้ ใช้คำนวณจำนวนขายรวม (`qty`) | `12.0` |
| `sopricunit` | `decimal` | ราคาขายป้ายต่อหน่วย (Unit Price) ใช้คำนวณส่วนลดบิลผิดปกติ | `17.00` |
| `socstunit` | `decimal` | ต้นทุนต่อหน่วยจริง (Unit Cost) ใช้ตรวจสอบรายการที่ทุนเกินราคาขาย | `15.41` |
| `solineamt` | `decimal` | ยอดขายระดับรายการก่อนหักส่วนลดท้ายบิล ใช้คำนวณส่วนลดท้ายบิล | `204.00` |
| `net_sales_amt` | `decimal` | **ยอดขายสุทธิที่แท้จริง** หลังหักส่วนลดท้ายบิลเฉลี่ยลงรายการ (**ใช้เป็นยอดขายหลัก**) | `0.00` |
| `total_cost` | `decimal` | ต้นทุนขายรวมของรายการนั้นๆ (`soqty * socstunit`) ใช้คำนวณ Cost และ GP | `184.92` |
| `sopricdisc` | `decimal` | ส่วนลดระดับรายการต่อหน่วย (Line Discount per Unit) | `17.00` |
| `solinetype` | `char(1)` | ประเภทบรรทัดบิล กรองทิ้งหากเป็น `'C'` (Cancel) หรือ `'R'` (Return) | `'N'` |
| `soretflag` | `char(1)` | ตัวระบุการคืนสินค้า กรองเฉพาะบิลขายปกติ (`soretflag = 'N'`) | `'N'` |

*Index ที่สำคัญ:*
- ตารางนี้ต้องระบุ `FORCE INDEX (idx_optimize_sales_report)` ใน SQL Statement เพื่อป้องกัน Query Timeout เนื่องจากตารางมีขนาดมหาศาล

---

#### ตารางที่ 2: `data-lake.dim_branch` (ตาราง Master สาขา)
*บทบาท:* เก็บโครงสร้างสาขาและสายการบังคับบัญชาการค้าปลีก

| ชื่อคอลัมน์จริง | ชนิดข้อมูล | บทบาทและหน้าที่ในระบบ | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `code` | `varchar / int` | รหัสสาขา นำมาแปลงเป็นฟอร์แมต 3 หลัก เช่น `016`, `311` เพื่อผูกกับ `sotowhs` | `'311'` |
| `name` | `varchar` | ชื่อสาขาภาษาไทย ใช้แสดงผลในทุกตารางและดรอปดาวน์ | `'ทุ่งเสลี่ยม'` |
| `dm` | `varchar` | ชื่อผู้จัดการเขต (District Manager: DM) ใช้จัดกลุ่มและฟิลเตอร์ระดับเขต | `'DM 05'` |
| `rm` | `varchar` | ชื่อผู้จัดการภาค (Regional Manager: RM) ใช้จัดกลุ่มและฟิลเตอร์ระดับภาค | `'RM 1'` |

---

#### ตารางที่ 3: `data-lake.dim_product` (ตาราง Master สินค้า)
*บทบาท:* เก็บรายละเอียดสินค้าและต้นทุนมาตรฐานทางบัญชี

| ชื่อคอลัมน์จริง | ชนิดข้อมูล | บทบาทและหน้าที่ในระบบ | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `iprod` | `varchar` | รหัสสินค้าหลัก (Master Product Code) | `'100000000000000212'` |
| `idesc` | `varchar` | ชื่อสินค้าภาษาไทย | `'โค้ก 500มล.'` |
| `igrcode` | `varchar` | รหัสกลุ่มสินค้า 5 หลัก นำ 2 หลักแรกมาเช็คหมวดหมู่ (เช่น `22` = เทศกาล, `02` = มินิมาร์ท) | `'22001'`, `'02101'` |
| `iacst` | `decimal` | ต้นทุนเฉลี่ยตามบัญชี (Actual Cost) ใช้ตรวจสินค้าที่ต้นทุนเกินราคาขาย | `15.41` |
| `ipunit3` | `decimal` | ราคาขายป้าย Price 3 ใช้เป็นราคาอ้างอิงเทียบกับต้นทุน `iacst` | `17.00` |

---

### 2.2 ฐานข้อมูล: `MYPOS2018_CENTER`
- **สิทธิ์การเข้าถึง:** **READ-ONLY 100%**
- **Charset / Collation:** `utf8mb3`
- **บทบาททางสถาปัตยกรรม:** เป็นฐานข้อมูลระบบ POS หน้าร้าน ใช้สำหรับแปลงรหัส (Lookup) และจับคู่บาร์โค้ด (Barcode Mapping)

#### ตารางที่ 1: `MYPOS2018_CENTER.item_group` (ตารางกลุ่มสินค้าย่อย)
*บทบาท:* แปลงรหัสกลุ่มย่อย 5 หลักเป็นชื่อภาษาไทย

| ชื่อคอลัมน์จริง | บทบาทและหน้าที่ในระบบ | ตัวอย่างข้อมูล |
| :--- | :--- | :--- |
| `igrcode` | รหัสกลุ่มสินค้าย่อย 5 หลัก (ตรงกับ `dim_product.igrcode`) | `'02101'` |
| `igrdesc` | ชื่อกลุ่มสินค้าย่อยภาษาไทย ใช้แสดงผลในตารางโครงสร้าง Minimart | `'เครื่องดื่มไม่มีแอลกอฮอล์'` |

---

#### ตารางที่ 2: `MYPOS2018_CENTER.item_type` (ตารางประเภทสินค้าหลัก)
*บทบาท:* แปลงรหัสประเภทสินค้า 2 หลักแรกเป็นชื่อประเภทภาษาไทย

| ชื่อคอลัมน์จริง | บทบาทและหน้าที่ในระบบ | ตัวอย่างข้อมูล |
| :--- | :--- | :--- |
| `itycode` | รหัสประเภทสินค้า 2 หลัก | `'02'`, `'22'` |
| `itydesc` | ชื่อประเภทสินค้าภาษาไทย | `'สินค้ามินิมาร์ท'`, `'สินค้าเทศกาล'` |

---

#### ตารางที่ 3: `MYPOS2018_CENTER.item_barcode` (ตารางจับคู่บาร์โค้ด Alias)
*บทบาท:* Map รหัสบาร์โค้ดที่แคชเชียร์ยิงหน้าร้านกลับมาเป็นรหัสสินค้าหลัก

| ชื่อคอลัมน์จริง | บทบาทและหน้าที่ในระบบ | เงื่อนไขการใช้งาน |
| :--- | :--- | :--- |
| `barcode` | บาร์โค้ดที่ยิงหน้าร้าน (Barcode Alias) | เฉพาะรายการที่ `baractive = 'Y'` |
| `parcode` | รหัสสินค้าหลักที่เป็น Parent Master (`master iprod`) | นำไปแทนที่ `iprod` กรณีที่ `iprod` ไม่พบใน Master |

---

### 2.3 ข้อควรระวังเรื่อง Collation Mismatch ข้ามฐานข้อมูล (Cross-Schema Collation Handling)

> [!CAUTION]
> ⚠️ **คำเตือนสำคัญสำหรับทีม IT (Database Administrator & Data Engineer):**
> ระหว่างฐานข้อมูล `data-lake` และ `MYPOS2018_CENTER` มีการกำหนด Character Set และ Collation ต่างกัน:
> - **`data-lake.*`**: ใช้ `utf8mb4` (Collation: `utf8mb4_general_ci`)
> - **`MYPOS2018_CENTER.*`**: ใช้ `utf8mb3` / `utf8` (Collation: `utf8mb3_general_ci`)
>
> หากนำตารางข้ามฐานข้อมูลมาทำ `JOIN` กันตรงๆ ผ่าน SQL เช่น:
> ```sql
> -- ❌ คำสั่งนี้จะพังทันทีด้วย Error 1267!
> SELECT *
> FROM `data-lake`.fact_sales fs
> JOIN `MYPOS2018_CENTER`.item_barcode ib ON ib.barcode = fs.iprod;
> ```
> MySQL จะตัดการทำงานทันทีและส่ง Error:
> `ERROR 1267 (HY000): Illegal mix of collations (utf8mb4_general_ci,IMPLICIT) and (utf8mb3_general_ci,IMPLICIT) for operation '='`

#### แนวทางการแก้ไขในคำสั่ง SQL (SQL Solutions):
หากจำเป็นต้องเขียนคำสั่ง SQL JOIN ข้ามระบบโดยตรง สามารถทำได้ 2 วิธี:

1. **วิธีที่ 1: ใช้ `CAST(... AS BINARY)` (แนะนำ — ปลอดภัยและเร็วที่สุด)**
   แปลงทั้งสองฝั่งให้เปรียบเทียบในระดับไบต์ (Binary string):
   ```sql
   ON CAST(ib.barcode AS BINARY) = CAST(fs.iprod AS BINARY)
   ```
2. **วิธีที่ 2: ระบุ `COLLATE utf8mb4_general_ci` ชัดเจน**
   แปลง Collation ของฝั่ง `utf8mb3` ให้เป็น `utf8mb4`:
   ```sql
   ON (ib.barcode COLLATE utf8mb4_general_ci) = fs.iprod
   ```

#### สถาปัตยกรรม Python Pipeline (`build_gp_analysis.py`) หลีกเลี่ยงปัญหานี้อย่างไร?
ในสคริปต์ `build_gp_analysis.py` ระบบถูกออกแบบให้ **ไม่ทำ Cross-Schema JOIN บน MySQL Database Engine**:
1. สคริปต์จะ Query `dim_product` และ `item_barcode` แยกออกมาเก็บไว้ใน Python In-Memory Dictionary (`prods`, `barcodes`)
2. จากนั้น Query `fact_sales` แล้วนำข้อมูลมา Resolve จับคู่ Barcode Alias และ Master Product ใน RAM ผ่าน Python Dictionary lookup แบบ $O(1)$
3. **ข้อดี:**
   - ขจัดปัญหา Collation Conflict 100%
   - ลดภาระหนักของ MySQL Server (ไม่ต้อง Cross-Join ตารางขนาดหลายสิบล้านแถว)
   - ประมวลผลได้รวดเร็ว (Pipeline ใช้เวลาเฉลี่ยไม่ถึง 45 วินาที)

---

## 3. คลังข้อมูลระดับไฟล์: `gp_analysis_data.json` (Local Datamart)

- **ที่ตั้งไฟล์:** `F:\lost-Product\gp_analysis_data.json` (และสำเนาซิงค์ที่ `F:\facebook\gp_data.json`)
- **ขนาดไฟล์:** ประมาณ ~8.3 MB
- **บทบาททางสถาปัตยกรรม:**
  ทำหน้าที่เป็น **Pre-aggregated Analytical Datamart** โดยรวบรวมและสรุปผลข้อมูลทั้งหมดจาก MySQL Server ให้พร้อมใช้งานทันที
- **โครงสร้างข้อมูลภายใน (JSON Schema):**
  1. `_meta`: ข้อมูลระบุวันที่สร้าง, จำนวนวันที่ผ่านไปในเดือน (`days_elapsed`), เดือนที่วิเคราะห์ (`months`)
  2. `summary`: ยอดรวมระดับองค์กร (`sales`, `cost`, `disc`, `gp`, `gp_pct`, `store_count`, `product_count`)
  3. `monthly`: ยอดขายและ GP แยกตามเดือน สำหรับทำกราฟและตาราง Trend
  4. `stores`: ยอดขายและ GP แยกตามสาขาทั้ง 202 สาขา พร้อมโครงสร้าง RM/DM
  5. `products`: ยอดขายและ GP แยกตามสินค้าทั้งหมด 11,135 SKUs พร้อมระบุกลุ่มและประเภท
  6. `prod_stores`: ยอดขายแยกรายสาขาสำหรับสินค้าที่มีปัญหา GP% ต่ำกว่า 15%
  7. `festival_stores`: สรุปข้อมูลสินค้าเทศกาล (Category 22) แยกรายสาขาสำหรับ Tree View
  8. `anomalies`: รายการบิลขายขาดทุน 250 รายการ และสินค้าที่ต้นทุนเกินราคาป้าย 97 รายการ
  9. `branch_901`: ข้อมูลเฉพาะของร้าน Online (สาขา 901) ทั้งยอดรายเดือน, สินค้าขายดี, และบิลผิดปกติ

---

## 4. สรุปหน้าที่และการใช้งานตามฟังก์ชันของแดชบอร์ด

| หน้า / ฟังก์ชันในแดชบอร์ด | ฐานข้อมูลที่ใช้งาน | ตารางที่ใช้งาน | วัตถุประสงค์ในการดึงข้อมูล |
| :--- | :--- | :--- | :--- |
| **ภาพรวม KPI องค์กร (5 Cards ด้านบน)** | `data-lake` | `fact_sales` | คำนวณ Net Sales, Cost, Discount, GP และ GP% MTD เปรียบเทียบเดือนก่อน |
| **แท็บ 1: 🏪 Store Overview** | `data-lake` | `fact_sales`, `dim_branch` | วิเคราะห์ยอดขายและกำไรขั้นต้นรายสาขา พร้อมจัดกลุ่มตาม RM และ DM |
| **แท็บ 2: 📦 Product Overview** | `data-lake`, `MYPOS2018_CENTER` | `fact_sales`, `dim_product`, `item_group`, `item_type`, `item_barcode` | วิเคราะห์ GP รายสินค้า และแปลงบาร์โค้ดลูกเป็นรหัสสินค้าหลัก |
| **แท็บ 3: 📈 Monthly Trend** | `data-lake` | `fact_sales` | สร้างกราฟแท่งเปรียบเทียบยอดขายและต้นทุน พร้อมเส้น GP% รายเดือน |
| **แท็บ 4: 🎪 วิกฤตสินค้าเทศกาล (Cat 22)** | `data-lake` | `fact_sales`, `dim_product`, `dim_branch` | คำนวณผลกระทบของของแจก/แถมฟรี Category 22 ที่ฉุด GP ลง -7.09 pp |
| **แท็บ 5: 🛒 โครงสร้าง Minimart (Cat 02)** | `data-lake`, `MYPOS2018_CENTER` | `fact_sales`, `dim_product`, `item_group` | วิเคราะห์กลุ่มย่อยใน Minimart ที่มาร์จิ้นต่ำ ฉุด GP รวม -3.69 pp |
| **แท็บ 6: 💡 จำลอง Pro-Forma GP** | `data-lake` | `fact_sales`, `dim_branch` | นำข้อมูลต้นทุน Category 22 มาคำนวณการจัดสรรงบประมาณการตลาดราย RM/DM |
| **แท็บ 8: ⚠️ ตรวจสอบต้นทุน & บิลผิดปกติ** | `data-lake` | `fact_sales`, `dim_product`, `dim_branch` | ดึง 250 บิลขาดทุนสูงสุด และตรวจหาสินค้าที่ต้นทุนเฉลี่ยเกินราคาขายป้าย |
| **แท็บ 9: 🌐 ร้าน Online (สาขา 901)** | `data-lake` | `fact_sales`, `dim_product` | ดึงข้อมูลเฉพาะของสาขา 901 ออกมาแสดงผล ไม่ให้ปนกับสาขาหน้าร้าน |

---

## 5. มาตรการความปลอดภัยและประสิทธิภาพ (Security & Performance)

1. **สิทธิ์การเข้าถึงเป็นแบบ Read-Only ทั้งหมด:**
   การเชื่อมต่อไปยัง `data-lake` และ `MYPOS2018_CENTER` ทำการตั้งค่า User ที่มีสิทธิ์เฉพาะ `SELECT` เท่านั้น จึงไม่มีความเสี่ยงเรื่องข้อมูลสูญหาย หรือถูกดัดแปลงจากระบบนี้
2. **การปกป้องฐานข้อมูล Production (Zero Load at Runtime):**
   ผู้ใช้งาน Dashboard กว่าหลายสิบท่านจะเปิดอ่านข้อมูลจากไฟล์ `gp_analysis_data.json` บน Client Browser โดยตรง ไม่มีการยิงคำสั่ง SQL ไปยัง Production Server ในระหว่างวัน
3. **การรัน ETL Pipeline ในช่วงเวลาปิดรอบ:**
   สคริปต์ `build_gp_analysis.py` ถูกตั้งเวลาให้ทำงานวันละ 1 ครั้งในช่วงเช้าตรู่ (06:00 น.) ซึ่งเป็นช่วงเวลาที่การใช้งานระบบ POS หน้าร้านต่ำสุด และใช้เวลารันเพียง 30 - 45 วินาที
